"""GeometryRegularizer implementation(s).

No third-party library to adapt here either — same situation as
calibration/ and vectorization/. Kept alongside io/, vision/, ml/,
export/, and persistence/ anyway, so every Protocol implementation lives
in one place.
"""